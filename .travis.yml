language: node_js

# Use container-based infrastructure
sudo: false

# node versions to test
node_js:
  - "4"    # latest LTS version
  - "5"    # latest version

# If any version fails, cancel the others
matrix:
  fast_finish: true

before_script:
  - npm install -g gulp-cli istanbul mocha

script: gulp
